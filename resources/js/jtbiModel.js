//TODO: on window resize
jtbiApp.jtbiModel = (function(elements) {

    let that = {}, events = [];

    function init() {
        //tileArray = Array.prototype.slice.call(elements.activeMeGrid.querySelectorAll("div.tile"));
    }
    


    that.init = init;
    return that;
});